name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: submodules
      run: git checkout master && git submodule init && git submodule update
    - name: make linux
      run: make depends target=x86_64-linux-gnu
